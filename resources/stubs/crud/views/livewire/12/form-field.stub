    <div>
        <flux:input
            wire:model="form.{{column}}"
            :label="__('{{title}}')"
            type="text"
            autocomplete="form.{{column}}"
            :placeholder="__('{{title}}')"
        />
    </div>
