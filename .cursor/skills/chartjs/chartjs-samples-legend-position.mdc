---
alwaysApply: false
description: Documentación local (chartjs): Position (/docs/latest/samples/legend/position.html). Usar como referencia al implementar o depurar según esta documentación.
---

# Position

> Fuente: https://www.chartjs.org/docs/latest/samples/legend/position.html
> Exportado: 2026-07-12 20:58 UTC

# [#](#position) Position

This sample show how to change the position of the chart legend.

config
setup
actions

const config = {
type: 'line',
data: data,
};

```
const config = {
  type: 'line',
  data: data,
};
```

const DATA\_COUNT = 7;
const NUMBER\_CFG = {count: DATA\_COUNT, min: -100, max: 100};
const data = {
labels: Utils.months({count: DATA\_COUNT}),
datasets: [
{
label: 'Dataset 1',
data: Utils.numbers(NUMBER\_CFG),
fill: false,
borderColor: Utils.CHART\_COLORS.red,
backgroundColor: Utils.transparentize(Utils.CHART\_COLORS.red, 0.5),
},
]
};

```
const DATA_COUNT = 7;
const NUMBER_CFG = {count: DATA_COUNT, min: -100, max: 100};
const data = {
  labels: Utils.months({count: DATA_COUNT}),
  datasets: [
    {
      label: 'Dataset 1',
      data: Utils.numbers(NUMBER_CFG),
      fill: false,
      borderColor: Utils.CHART_COLORS.red,
      backgroundColor: Utils.transparentize(Utils.CHART_COLORS.red, 0.5),
    },
  ]
};
```

const actions = [
{
name: 'Position: top',
handler(chart) {
chart.options.plugins.legend.position = 'top';
chart.update();
}
},
{
name: 'Position: right',
handler(chart) {
chart.options.plugins.legend.position = 'right';
chart.update();
}
},
{
name: 'Position: bottom',
handler(chart) {
chart.options.plugins.legend.position = 'bottom';
chart.update();
}
},
{
name: 'Position: left',
handler(chart) {
chart.options.plugins.legend.position = 'left';
chart.update();
}
},
];

```
const actions = [
  {
    name: 'Position: top',
    handler(chart) {
      chart.options.plugins.legend.position = 'top';
      chart.update();
    }
  },
  {
    name: 'Position: right',
    handler(chart) {
      chart.options.plugins.legend.position = 'right';
      chart.update();
    }
  },
  {
    name: 'Position: bottom',
    handler(chart) {
      chart.options.plugins.legend.position = 'bottom';
      chart.update();
    }
  },
  {
    name: 'Position: left',
    handler(chart) {
      chart.options.plugins.legend.position = 'left';
      chart.update();
    }
  },
];
```

## [#](#docs) Docs

- [Data structures (`labels`)](/docs/latest/general/data-structures.html)
- [Line](/docs/latest/charts/line.html)
- [Legend](/docs/latest/configuration/legend.html)
  - [Position](/docs/latest/configuration/legend.html#position)
