---
alwaysApply: false
description: Documentación local (chartjs): Using from Node.js (/docs/latest/getting-started/using-from-node-js.html). Usar como referencia al implementar o depurar según esta documentación.
---

# Using from Node.js

> Fuente: https://www.chartjs.org/docs/latest/getting-started/using-from-node-js.html
> Exportado: 2026-07-12 20:58 UTC

# [#](#using-from-node-js) Using from Node.js

You can use Chart.js in Node.js for server-side generation of plots with help from an NPM package such as [node-canvas  (opens new window)](https://github.com/Automattic/node-canvas) or [skia-canvas  (opens new window)](https://skia-canvas.org/).

Sample usage:

```
import {CategoryScale, Chart, LinearScale, LineController, LineElement, PointElement} from 'chart.js';
import {Canvas} from 'skia-canvas';
import fsp from 'node:fs/promises';
Chart.register([
  CategoryScale,
  LineController,
  LineElement,
  LinearScale,
  PointElement
]);
const canvas = new Canvas(400, 300);
const chart = new Chart(
  canvas, // TypeScript needs "as any" here
  {
    type: 'line',
    data: {
      labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
      datasets: [{
        label: '# of Votes',
        data: [12, 19, 3, 5, 2, 3],
        borderColor: 'red'
      }]
    }
  }
);
const pngBuffer = await canvas.toBuffer('png', {matte: 'white'});
await fsp.writeFile('output.png', pngBuffer);
chart.destroy();
```
