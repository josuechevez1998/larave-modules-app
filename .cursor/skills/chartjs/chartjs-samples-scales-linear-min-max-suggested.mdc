---
alwaysApply: false
description: Documentación local (chartjs): Linear Scale - Suggested Min-Max (/docs/latest/samples/scales/linear-min-max-suggested.html). Usar como referencia al implementar o depurar según esta documentación.
---

# Linear Scale - Suggested Min-Max

> Fuente: https://www.chartjs.org/docs/latest/samples/scales/linear-min-max-suggested.html
> Exportado: 2026-07-12 20:59 UTC

# [#](#linear-scale-suggested-min-max) Linear Scale - Suggested Min-Max

config
setup

const config = {
type: 'line',
data: data,
options: {
responsive: true,
plugins: {
title: {
display: true,
text: 'Suggested Min and Max Settings'
}
},
scales: {
y: {
// the data minimum used for determining the ticks is Math.min(dataMin, suggestedMin)
suggestedMin: 30,
// the data maximum used for determining the ticks is Math.max(dataMax, suggestedMax)
suggestedMax: 50,
}
}
},
};

```
const config = {
  type: 'line',
  data: data,
  options: {
    responsive: true,
    plugins: {
      title: {
        display: true,
        text: 'Suggested Min and Max Settings'
      }
    },
    scales: {
      y: {
        // the data minimum used for determining the ticks is Math.min(dataMin, suggestedMin)
        suggestedMin: 30,
        // the data maximum used for determining the ticks is Math.max(dataMax, suggestedMax)
        suggestedMax: 50,
      }
    }
  },
};
```

const DATA\_COUNT = 7;
const NUMBER\_CFG = {count: DATA\_COUNT, min: 0, max: 100};
const labels = Utils.months({count: 7});
const data = {
labels: labels,
datasets: [
{
label: 'Dataset 1',
data: [10, 30, 39, 20, 25, 34, -10],
borderColor: Utils.CHART\_COLORS.red,
backgroundColor: Utils.CHART\_COLORS.red,
},
{
label: 'Dataset 2',
data: [18, 33, 22, 19, 11, 39, 30],
borderColor: Utils.CHART\_COLORS.blue,
backgroundColor: Utils.CHART\_COLORS.blue,
}
]
};

```
const DATA_COUNT = 7;
const NUMBER_CFG = {count: DATA_COUNT, min: 0, max: 100};
const labels = Utils.months({count: 7});
const data = {
  labels: labels,
  datasets: [
    {
      label: 'Dataset 1',
      data: [10, 30, 39, 20, 25, 34, -10],
      borderColor: Utils.CHART_COLORS.red,
      backgroundColor: Utils.CHART_COLORS.red,
    },
    {
      label: 'Dataset 2',
      data: [18, 33, 22, 19, 11, 39, 30],
      borderColor: Utils.CHART_COLORS.blue,
      backgroundColor: Utils.CHART_COLORS.blue,
    }
  ]
};
```

## [#](#docs) Docs

- [Line](/docs/latest/charts/line.html)
- [Data structures (`labels`)](/docs/latest/general/data-structures.html)
- [Axes scales](/docs/latest/axes/)
  - [Common options to all axes](/docs/latest/axes/#common-options-to-all-axes)
  - [Axis Range Settings](/docs/latest/axes/#axis-range-settings)
