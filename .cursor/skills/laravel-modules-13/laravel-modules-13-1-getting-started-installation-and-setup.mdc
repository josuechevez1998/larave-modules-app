---
alwaysApply: false
description: Documentación local (laravel-modules-13): Installation (/docs/1/getting-started/installation-and-setup). Usar como referencia al implementar o depurar según esta documentación.
---

# Installation

> Fuente: https://laravelmodules.com/docs/1/getting-started/installation-and-setup
> Exportado: 2026-07-23 20:01 UTC

## [#](#composer)Composer

To install through composer, by run the following command:

Copied!

```
composer require nwidart/laravel-modules

composer require nwidart/laravel-modules
```

## [#](#add-service-provider)Add Service Provider

Next add the following service provider in `config/app.php`.

Copied!

```
'providers' => [

Nwidart\Modules\LaravelModulesServiceProvider::class,

],

'providers' => [
  Nwidart\Modules\LaravelModulesServiceProvider::class,
],
```

Next, add the following aliases to `aliases` array in the same file.

Copied!

```
'aliases' => [

'Module' => Nwidart\Modules\Facades\Module::class,

],

'aliases' => [
  'Module' => Nwidart\Modules\Facades\Module::class,
],
```

Next publish the package's configuration file by running :

Copied!

```
php artisan vendor:publish --provider="Nwidart\Modules\LaravelModulesServiceProvider"

php artisan vendor:publish --provider="Nwidart\Modules\LaravelModulesServiceProvider"
```

## [#](#autoloading)Autoloading

By default the module classes are not loaded automatically. You can autoload your modules using `psr-4`. For example :

Copied!

```
{

"autoload": {

"psr-4": {

"App\\": "app/",

"Modules\\": "Modules/"

}

}

}

{
  "autoload": {
    "psr-4": {
      "App\\": "app/",
      "Modules\\": "Modules/"
    }
  }
}
```

**Tip: don't forget to run `composer dump-autoload` afterwards**
