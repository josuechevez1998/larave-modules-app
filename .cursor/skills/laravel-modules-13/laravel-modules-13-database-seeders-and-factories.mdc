---
alwaysApply: false
description: Documentación local (laravel-modules-13): Seeders and Factories (/docs/13/database/seeders-and-factories). Referencia nwidart/laravel-modules v13.
---

# Seeders and Factories

> Fuente: https://laravelmodules.com/docs/13/database/seeders-and-factories
> Exportado: 2026-07-23 19:55 UTC

- [Generating Seeders](#generating-seeders)
- [The Master Database Seeder](#the-master-database-seeder)
- [Running Seeders](#running-seeders)
- [Generating Factories](#generating-factories)
- [Factory States](#factory-states)
- [Factory Relationships](#factory-relationships)
- [Using Factories in Seeders](#using-factories-in-seeders)
- [Calling the Main Application DatabaseSeeder](#calling-the-main-application-databaseseeder)

[![David Carr Laravel Developer](https://laravelmodules.com/images/sponsors/dcblog.png "David Carr Laravel Developer")](https://dcblog.dev?utm_source=laravelmodules.com&utm_medium=website)

# Seeders and Factories

---

- [Generating Seeders](#generating-seeders)
- [The Master Database Seeder](#the-master-database-seeder)
- [Running Seeders](#running-seeders)
- [Generating Factories](#generating-factories)
- [Factory States](#factory-states)
- [Factory Relationships](#factory-relationships)
- [Using Factories in Seeders](#using-factories-in-seeders)
- [Calling the Main Application DatabaseSeeder](#calling-the-main-application-databaseseeder)

Each module can have its own seeders and factories in `database/seeders/` and `database/factories/`.

## [#](#generating-seeders)Generating Seeders

```
php artisan module:make-seed PostSeeder Blog

php artisan module:make-seed PostSeeder Blog
```

This creates `Modules/Blog/database/seeders/PostSeeder.php`:

```
<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;

use Modules\Blog\Models\Post;

class PostSeeder extends Seeder

{

public function run(): void

{

Post::factory()->count(50)->create();

}

}

<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;
use Modules\Blog\Models\Post;

class PostSeeder extends Seeder
{
    public function run(): void
    {
        Post::factory()->count(50)->create();
    }
}
```

### [#](#the-master-database-seeder)The Master Database Seeder

When a module is scaffolded, a `BlogDatabaseSeeder.php` is generated. This is the entry-point seeder for the module and should call any other seeders the module needs:

```
<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;

class BlogDatabaseSeeder extends Seeder

{

public function run(): void

{

$this->call([

CategorySeeder::class,

PostSeeder::class,

]);

}

}

<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;

class BlogDatabaseSeeder extends Seeder
{
    public function run(): void
    {
        $this->call([
            CategorySeeder::class,
            PostSeeder::class,
        ]);
    }
}
```

Generate an explicit master seeder with the `--master` flag:

```
php artisan module:make-seed BlogDatabaseSeeder Blog --master

php artisan module:make-seed BlogDatabaseSeeder Blog --master
```

## [#](#running-seeders)Running Seeders

Run the seeder for a specific module:

```
php artisan module:seed Blog

php artisan module:seed Blog
```

This runs the module's master seeder (`BlogDatabaseSeeder`). Run a specific class:

```
php artisan module:seed Blog --class=PostSeeder

php artisan module:seed Blog --class=PostSeeder
```

Seed all modules at once:

```
php artisan module:seed

php artisan module:seed
```

Seed after migration:

```
php artisan module:migrate Blog --seed

php artisan module:migrate-refresh Blog --seed

php artisan module:migrate Blog --seed
php artisan module:migrate-refresh Blog --seed
```

## [#](#generating-factories)Generating Factories

```
php artisan module:make-factory PostFactory Blog

php artisan module:make-factory PostFactory Blog
```

This creates `Modules/Blog/database/factories/PostFactory.php`:

```
<?php

namespace Modules\Blog\Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;

use Modules\Blog\Models\Post;

class PostFactory extends Factory

{

protected $model = Post::class;

public function definition(): array

{

return [

'title'        => fake()->sentence(),

'slug'         => fake()->unique()->slug(),

'excerpt'      => fake()->paragraph(),

'body'         => fake()->paragraphs(5, true),

'status'       => 'published',

'published_at' => fake()->dateTimeBetween('-1 year', 'now'),

];

}

}

<?php

namespace Modules\Blog\Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Modules\Blog\Models\Post;

class PostFactory extends Factory
{
    protected $model = Post::class;

    public function definition(): array
    {
        return [
            'title'        => fake()->sentence(),
            'slug'         => fake()->unique()->slug(),
            'excerpt'      => fake()->paragraph(),
            'body'         => fake()->paragraphs(5, true),
            'status'       => 'published',
            'published_at' => fake()->dateTimeBetween('-1 year', 'now'),
        ];
    }
}
```

## [#](#factory-states)Factory States

Factory states let you create model variations for different test scenarios:

```
public function definition(): array

{

return [

'title'        => fake()->sentence(),

'slug'         => fake()->unique()->slug(),

'body'         => fake()->paragraphs(5, true),

'status'       => 'published',

'published_at' => now(),

];

}

public function draft(): static

{

return $this->state([

'status'       => 'draft',

'published_at' => null,

]);

}

public function scheduled(): static

{

return $this->state([

'status'       => 'scheduled',

'published_at' => fake()->dateTimeBetween('+1 day', '+1 month'),

]);

}

public function definition(): array
{
    return [
        'title'        => fake()->sentence(),
        'slug'         => fake()->unique()->slug(),
        'body'         => fake()->paragraphs(5, true),
        'status'       => 'published',
        'published_at' => now(),
    ];
}

public function draft(): static
{
    return $this->state([
        'status'       => 'draft',
        'published_at' => null,
    ]);
}

public function scheduled(): static
{
    return $this->state([
        'status'       => 'scheduled',
        'published_at' => fake()->dateTimeBetween('+1 day', '+1 month'),
    ]);
}
```

Use states in tests:

```
// Create a published post

$post = Post::factory()->create();

// Create a draft

$post = Post::factory()->draft()->create();

// Create 10 scheduled posts

$posts = Post::factory()->scheduled()->count(10)->create();

// Create a published post
$post = Post::factory()->create();

// Create a draft
$post = Post::factory()->draft()->create();

// Create 10 scheduled posts
$posts = Post::factory()->scheduled()->count(10)->create();
```

## [#](#factory-relationships)Factory Relationships

Create related models together using `for()` and `has()`:

```
// Create a post for a specific user

$post = Post::factory()

->for($user)

->create();

// Create a post with 5 comments

$post = Post::factory()

->has(Comment::factory()->count(5), 'comments')

->create();

// Create a post for a specific user
$post = Post::factory()
    ->for($user)
    ->create();

// Create a post with 5 comments
$post = Post::factory()
    ->has(Comment::factory()->count(5), 'comments')
    ->create();
```

Or use `recycle` to reuse an existing model:

```
$category = Category::factory()->create();

$posts = Post::factory()->count(10)->recycle($category)->create();

$category = Category::factory()->create();

$posts = Post::factory()->count(10)->recycle($category)->create();
```

## [#](#using-factories-in-seeders)Using Factories in Seeders

Factories are designed to be used in seeders for populating a database with realistic data:

```
<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;

use Modules\Blog\Models\Category;

use Modules\Blog\Models\Post;

use App\Models\User;

class BlogDatabaseSeeder extends Seeder

{

public function run(): void

{

$categories = Category::factory()->count(5)->create();

$authors = User::factory()->count(3)->create();

Post::factory()

->count(50)

->recycle($categories)

->recycle($authors)

->create();

Post::factory()

->count(10)

->draft()

->recycle($authors)

->create();

}

}

<?php

namespace Modules\Blog\Database\Seeders;

use Illuminate\Database\Seeder;
use Modules\Blog\Models\Category;
use Modules\Blog\Models\Post;
use App\Models\User;

class BlogDatabaseSeeder extends Seeder
{
    public function run(): void
    {
        $categories = Category::factory()->count(5)->create();
        $authors = User::factory()->count(3)->create();

        Post::factory()
            ->count(50)
            ->recycle($categories)
            ->recycle($authors)
            ->create();

        Post::factory()
            ->count(10)
            ->draft()
            ->recycle($authors)
            ->create();
    }
}
```

## [#](#calling-the-main-application-databaseseeder)Calling the Main Application DatabaseSeeder

If you want module seeders to run when the main `php artisan db:seed` is called, add them to your application's `Database\Seeders\DatabaseSeeder`:

```
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder

{

public function run(): void

{

$this->call([

\Modules\Blog\Database\Seeders\BlogDatabaseSeeder::class,

\Modules\Shop\Database\Seeders\ShopDatabaseSeeder::class,

]);

}

}

<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        $this->call([
            \Modules\Blog\Database\Seeders\BlogDatabaseSeeder::class,
            \Modules\Shop\Database\Seeders\ShopDatabaseSeeder::class,
        ]);
    }
}
```

---

[Previous page: 
←
Models](/docs/13/database/models)
[Next page: 
Resources and Packages: Resources
 →](/docs/13/resources/resources)

---

[Edit this page on GitHub](https://github.com/laravel-modules-com/docs/edit/main/13/database/seeders-and-factories.md)

Laravel Package built by [Nicolas Widart](https://github.com/nwidart).

Maintained by [David Carr](https://dcblog.dev?utm_source=https://laravelmodules.com/docs/13/database/seeders-and-factories&utm_medium=docs&utm_campaign=Laravel Modules)
follow on X [@dcblogdev](https://twitter.com/dcblogdev)
